import React from 'react';
import Hero from "../Hero";
// keeping this component for future additions to the homepage

const Homepage = (props) => {

    return (
        <>
        <Hero />

        </>
    );
};

export default Homepage;